apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finquery
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: finquery
            port:
              number: 80
